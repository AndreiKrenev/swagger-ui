apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: swagger-ui-pv-claim
  labels:
    app: swagger-ui
spec:
  volumeMode: Filesystem
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 250Mi