apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swagger-ui
  namespace: swagger-ui
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: swagger-ui
                port:
                  number: 8080
