apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swagger
  namespace: default
  selfLink: /apis/networking.k8s.io/v1/namespaces/default/ingresses/swagger
status:
  loadBalancer:
    ingress:
      - hostname: >-
spec:
  rules:
    - http:
        paths:
          - path: /path
            pathType: Exact
            backend:
              service:
                name: swagger
                port:
                  number: 8080
